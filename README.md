# Self-Hosting-SecretNumber-Service
Self hosting service that implements a random number generator between a high and a low input, and a guess service that generates a high, low, or correct string output for a guessed number and a passed through secret number. Also uses a C# console application to call the service as a game. 
